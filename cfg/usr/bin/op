#!/data/data/com.termux/files/usr/bin/env bash
#
# Openssh helper
set -e

case "${1}" in
  g) ssh-keygen -t ed25519 -f "${HOME}/.ssh/android";;
  c) cp "${HOME}/.ssh/android.pub" /storage/emulated/0/atp;;
  u) cat /storage/emulated/0/atp/linux.pub > "${HOME}/.ssh/authorized_keys"
esac
